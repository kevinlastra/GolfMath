class Node
{
 private:
  enum Type = {eau, sable, herbe, NONE};
  Type type;
  int posX;
  int posY;
 public:
  Node(int,int,type);
  Node(const Node);
  ~Node();
  void operator=(const Node);

  Type getType();
  
};
